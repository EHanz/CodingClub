#include <iostream>
#include <fstream>
#include <string>

int main ()
{
  std :: string file_text;
  std :: ifstream myFile;
  myFile.open ("stringmatching.txt");

  while (getline (myFile, file_text))
    {
      
    }
  
}

